import { SearchMusicPipe } from './search-music.pipe';

describe('SearchMusicPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchMusicPipe();
    expect(pipe).toBeTruthy();
  });
});
