import { ConverToSpace1Pipe } from './conver-to-space1.pipe';

describe('ConverToSpace1Pipe', () => {
  it('create an instance', () => {
    const pipe = new ConverToSpace1Pipe();
    expect(pipe).toBeTruthy();
  });
});
